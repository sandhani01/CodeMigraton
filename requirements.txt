# Core schemas and validation
pydantic>=2.0.0

# Strict semantic version comparison (e.g. Version("5.0") > Version("4.2"))
packaging>=23.0

# API Service
fastapi>=0.110.0
uvicorn>=0.28.0

# Vector math (cosine similarity)
numpy>=1.26.0

# HTTP requests for LLM APIs
httpx>=0.27.0

# Environment variables (.env)
python-dotenv>=1.0.0
